version: "3"
services:
    voicenotifier:
        build:
          context: .
          dockerfile: Dockerfile
        environment:
            TOKEN: your token
        volumes:
            - discord_VoiceNotifier_data:/app/data
        restart: always

volumes:
  channel_data: